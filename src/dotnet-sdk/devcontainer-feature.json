{
    "name": "dotnet sdk",
    "id": "dotnet-sdk",
    "version": "1.0.2",
    "description": "add dotnet sdk",
    "containerEnv": {
        "DOTNET_ROOT": "/usr/local/dotnet",
        "PATH": "$PATH:$DOTNET_ROOT",
        "DOTNET_RUNNING_IN_CONTAINER": "true",
        "DOTNET_USE_POLLING_FILE_WATCHER": "true"
    },
    "options": {
        "versions": {
            "type": "string",
            "default": "lts",
            "description": "Enter additional .NET SDK versions, separated by commas. Use 'latest' for the latest version. 'lts' for the latest LTS version. 'X.Y' or 'X.Y.Z' for a specific version.'X.Y-preview' or 'X.Y-daily' for prereleases"
        },
        "workloads": {
            "type": "string",
            "default": "",
            "description": "Enter additional .NET SDK workloads, separated by commas"
        }
    }
}