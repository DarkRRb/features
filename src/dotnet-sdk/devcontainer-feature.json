{
    "name": "dotnet sdk",
    "id": "dotnet-sdk",
    "version": "0.0.5",
    "description": "add dotnet sdk",
    "containerEnv": {
        "DOTNET_ROOT": "$_REMOTE_USER_HOME/.dotnet",
        "PATH": "$PATH:$DOTNET_ROOT:$_REMOTE_USER_HOME/.dotnet/tools",
        "DOTNET_RUNNING_IN_CONTAINER": "true",
        "DOTNET_USE_POLLING_FILE_WATCHER": "true"
    },
    "options": {
        "versions": {
            "type": "string",
            "default": "lts",
            "description": "Enter additional .NET SDK versions, separated by commas\nUse 'latest' for the latest version\n'lts' for the latest LTS version\n'X.Y' or 'X.Y.Z' for a specific version\n'X.Y-preview' or 'X.Y-daily' for prereleases"
        },
        "workloads": {
            "type": "string",
            "default": "",
            "description": "Enter additional .NET SDK workloads, separated by commas"
        }
    }
}